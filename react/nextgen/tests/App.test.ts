// import { test, expect } from '@playwright/test';

// test('HiMom component', async ({ page }) => {
//   await page.goto('http://localhost:3000'); // Replace with the URL where your React app is running

//   const hiMomText = await page.$('.text');
//   expect(hiMomText).toHaveText('Hi mom');

//   const toggleButton = await page.$('.button');
//   await toggleButton?.click();

//   const hiddenHiMomText = await page.$('.text');
//   expect(hiddenHiMomText).toBeNull();
// });
